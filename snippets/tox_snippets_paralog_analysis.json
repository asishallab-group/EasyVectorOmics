{
    "Call to detect_neofunctionalization": {
        "prefix": "tox:detect_neofunctionalization",
        "body": [
            "call detect_neofunctionalization(ancestors, n_families, genes, n_axes, gene_to_fam, n_genes, thresholds, neofunc, ierr)"
        ],
        "description": "Detects neofunctionalization of paralogs"
    },

    "Call to detect_dosage_effect": {
        "prefix": "tox:detect_dosage_effect",
        "body": [
            "call detect_dosage_effect(ancestor, genes, n_genes, n_dims, sorted_gene_to_fam_perm, perm_first_paralog_idx, n_paralogs, filtered_paralogs_mask, n_mask_chunks, n_results, max_subset_size, work_arr_paralog_subsets, n_paralog_subsets, active_mask, temp_paralog_vector, ierr, max_angle, gain_gamma)"
        ],
        "description": "Detects dosage effect of subsets of paralogs"
    },

    "Call to detect_subfunctionalization": {
        "prefix": "tox:detect_subfunctionalization",
        "body": [
            "call detect_subfunctionalization(ancestor, genes, n_genes, n_dims, rdi_threshold, sorted_gene_to_fam_perm, perm_first_paralog_idx, n_paralogs, filtered_paralogs_mask, n_mask_chunks, n_results, max_subset_size, work_arr_paralog_subsets, n_paralog_subsets, active_mask, temp_paralog_vector, paralog_norms, sorted_paralog_norms_perm, temp_work_array, ierr)"
        ],
        "description": "Detects subfunctionalization of subsets of paralogs"
    },

    "Call to mask_chunk_count": {
        "prefix": "tox:mask_chunk_count",
        "body": [
            "call mask_chunk_count(n_paralogs, count)"
        ],
        "description": "Calculates the number of 32-bit chunks needed for bit mask encoding of subsets"
    },

    "Call to filter_paralogs_by_pattern_subfunctionalization": {
        "prefix": "tox:filter_paralogs_by_pattern_subfunctionalization",
        "body": [
            "call filter_paralogs_by_pattern_subfunctionalization(gene_angles, threshold, n_genes, sorted_gene_to_fam_perm, perm_first_paralog_idx, n_paralogs, mask, n_mask_chunks, ierr)"
        ],
        "description": "Prefilter the paralogs to reduce number of combinations in pattern detection"
    },

    "Call to filter_paralogs_by_pattern_dosage_effect": {
        "prefix": "tox:filter_paralogs_by_pattern_dosage_effect",
        "body": [
            "call filter_paralogs_by_pattern_dosage_effect(gene_angles, threshold, n_genes, sorted_gene_to_fam_perm, perm_first_paralog_idx, n_paralogs, mask, n_mask_chunks, ierr)"
        ],
        "description": "Prefilter the paralogs to reduce number of combinations in pattern detection"
    },

    "Call to calc_work_arr_paralog_subsets_size": {
        "prefix": "tox:calc_work_arr_paralog_subsets_size",
        "body": [
            "call calc_work_arr_paralog_subsets_size(max_subset_size, n_paralogs, work_array_size, filtered_paralogs_mask, n_mask_chunks, ierr)"
        ],
        "description": "Having the prefiltered bit mask for the paralogs, this routine calculates the needed work array size in pattern detection"
    },

    "Call to mask_set_state": {
        "prefix": "tox:mask_set_state",
        "body": [
            "call mask_set_state(bit_mask, i_paralog, state, ierr)"
        ],
        "description": "Sets the state of a paralog in the bit mask to enabled or disabled"
    },

    "Call to mask_check_state": {
        "prefix": "tox:mask_check_state",
        "body": [
            "mask_check_state(bit_mask, i_paralog)"
        ],
        "description": "Tells if a paralog is enabled or disabled in the bit mask"
    },

    "Python set up tox_detect_neofunctionalization": {
      "prefix": "tox:tox_detect_neofunctionalization_py",
      "body": [
        "def tox_detect_neofunctionalization(ancestors, genes, gene_to_fam, thresholds):",
        "    \"\"\"",
        "    Identify neofunctionalization for genes by checking whether the difference",
        "    of expression to its ancestor exceeds the threshold for each axis.",
        "",
        "    Args:",
        "        ancestors (np.ndarray): 2D array (n_axes, n_families).",
        "        genes (np.ndarray): 2D array (n_axes, n_genes).",
        "        gene_to_fam (np.ndarray): 1D int array mapping gene index to family index.",
        "        thresholds (np.ndarray): 1D float array (n_axes,) with per-axis thresholds.",
        "",
        "    Returns:",
        "        np.ndarray: Boolean array (n_genes, n_axes) with neofunctionalization flags.",
        "    \"\"\"",
        "    ancestors = np.asfortranarray(ancestors, dtype=np.float64)",
        "    genes = np.asfortranarray(genes, dtype=np.float64)",
        "    gene_to_fam = np.ascontiguousarray(gene_to_fam, dtype=np.int32)",
        "    thresholds = np.ascontiguousarray(thresholds, dtype=np.float64)",
        "",
        "    n_axes = ctypes.c_int(ancestors.shape[0])",
        "    n_families = ctypes.c_int(ancestors.shape[1])",
        "    n_genes = ctypes.c_int(genes.shape[1])",
        "",
        "    neofunc = np.empty((n_genes.value, n_axes.value), dtype=np.int32, order=\"F\")",
        "    ierr = ctypes.c_int(0)",
        "",
        "    detect_neofunc_c = lib.detect_neofunctionalization_c",
        "    detect_neofunc_c.argtypes = [",
        "        np.ctypeslib.ndpointer(dtype=np.float64, flags=\"F_CONTIGUOUS\"),",
        "        ctypes.POINTER(ctypes.c_int),",
        "        np.ctypeslib.ndpointer(dtype=np.float64, flags=\"F_CONTIGUOUS\"),",
        "        ctypes.POINTER(ctypes.c_int),",
        "        np.ctypeslib.ndpointer(dtype=np.int32, flags=\"C_CONTIGUOUS\"),",
        "        ctypes.POINTER(ctypes.c_int),",
        "        np.ctypeslib.ndpointer(dtype=np.float64, flags=\"C_CONTIGUOUS\"),",
        "        np.ctypeslib.ndpointer(dtype=np.int32, flags=\"F_CONTIGUOUS\"),",
        "        ctypes.POINTER(ctypes.c_int)",
        "    ]",
        "    detect_neofunc_c.restype = None",
        "",
        "    detect_neofunc_c(ancestors, ctypes.byref(n_families), genes, ctypes.byref(n_axes),",
        "                     gene_to_fam, ctypes.byref(n_genes), thresholds, neofunc, ctypes.byref(ierr))",
        "    check_err_code(ierr.value)",
        "",
        "    neofunc_bool = neofunc > 0",
        "    _readonly(neofunc)",
        "    return neofunc_bool",
        ""
      ],
      "description": "Sets up the python function to call tox_detect_neofunctionalization"
    },

    "Python set up tox_mask_check_state": {
      "prefix": "tox:tox_mask_check_state_py",
      "body": [
        "def tox_mask_check_state(bit_mask, i_paralog):",
        "    \"\"\"",
        "    Check the state of a specific paralog in a bit mask.",
        "",
        "    Args:",
        "        bit_mask (array-like): int32 mask chunks.",
        "        i_paralog (int): Index of the paralog to check.",
        "",
        "    Returns:",
        "        bool: True if active, False otherwise.",
        "    \"\"\"",
        "    bit_mask = np.ascontiguousarray(bit_mask, dtype=np.int32)",
        "    n_mask_chunks = ctypes.c_int(len(bit_mask))",
        "    i_paralog = ctypes.c_int(i_paralog)",
        "    state = ctypes.c_int(0)",
        "    ierr = ctypes.c_int(0)",
        "",
        "    mask_check_state_c = lib.mask_check_state_c",
        "    mask_check_state_c.argtypes = [",
        "        np.ctypeslib.ndpointer(dtype=np.int32, flags=\"C_CONTIGUOUS\"),",
        "        ctypes.POINTER(ctypes.c_int),",
        "        ctypes.POINTER(ctypes.c_int),",
        "        ctypes.POINTER(ctypes.c_int),",
        "        ctypes.POINTER(ctypes.c_int)",
        "    ]",
        "    mask_check_state_c.restype = None",
        "",
        "    mask_check_state_c(bit_mask, ctypes.byref(n_mask_chunks),",
        "                       ctypes.byref(i_paralog), ctypes.byref(state), ctypes.byref(ierr))",
        "    check_err_code(ierr.value)",
        "    return bool(state.value)",
        ""
      ],
      "description": "Sets up the python function to call tox_mask_check_state"
    },

    "Python set up tox_mask_chunk_count": {
      "prefix": "tox:tox_mask_chunk_count_py",
      "body": [
        "def tox_mask_chunk_count(n_paralogs):",
        "    \"\"\"",
        "    Compute the number of 32-bit chunks needed to encode paralogs.",
        "",
        "    Args:",
        "        n_paralogs (int): Number of paralogs.",
        "",
        "    Returns:",
        "        int: Number of chunks.",
        "    \"\"\"",
        "    n_paralogs = ctypes.c_int(n_paralogs)",
        "    count = ctypes.c_int(0)",
        "",
        "    mask_chunk_count_c = lib.mask_chunk_count_c",
        "    mask_chunk_count_c.argtypes = [",
        "        ctypes.POINTER(ctypes.c_int),",
        "        ctypes.POINTER(ctypes.c_int)",
        "    ]",
        "    mask_chunk_count_c.restype = None",
        "",
        "    mask_chunk_count_c(ctypes.byref(n_paralogs), ctypes.byref(count))",
        "    return count.value",
        ""
      ],
      "description": "Sets up the python function to call tox_mask_chunk_count"
    },

    "Python set up tox_calc_work_arr_paralog_subsets_size": {
      "prefix": "tox:tox_calc_work_arr_paralog_subsets_size_py",
      "body": [
        "def tox_calc_work_arr_paralog_subsets_size(max_subset_size, n_paralogs, filtered_paralogs_mask):",
        "    \"\"\"",
        "    Calculate the required work array size for paralog subset analysis.",
        "",
        "    Args:",
        "        max_subset_size (int): Maximum subset size (may be adjusted by the routine).",
        "        n_paralogs (int): Total number of paralogs.",
        "        filtered_paralogs_mask (array-like): Integer bit mask (chunks of 32 bits).",
        "",
        "    Returns:",
        "        dict: { 'actual_max_subset_size': int, 'work_array_size': int }",
        "    \"\"\"",
        "    filtered_paralogs_mask = np.ascontiguousarray(filtered_paralogs_mask, dtype=np.int32)",
        "    n_mask_chunks = ctypes.c_int(len(filtered_paralogs_mask))",
        "    n_paralogs = ctypes.c_int(n_paralogs)",
        "    max_subset_size_c = ctypes.c_int(max_subset_size)",
        "    work_array_size = ctypes.c_int(0)",
        "    ierr = ctypes.c_int(0)",
        "",
        "    calc_work_arr_size_c = lib.calc_work_arr_paralog_subsets_size",
        "    calc_work_arr_size_c.argtypes = [",
        "        ctypes.POINTER(ctypes.c_int),",
        "        ctypes.POINTER(ctypes.c_int),",
        "        ctypes.POINTER(ctypes.c_int),",
        "        np.ctypeslib.ndpointer(dtype=np.int32, flags=\"C_CONTIGUOUS\"),",
        "        ctypes.POINTER(ctypes.c_int),",
        "        ctypes.POINTER(ctypes.c_int)",
        "    ]",
        "    calc_work_arr_size_c.restype = None",
        "",
        "    calc_work_arr_size_c(ctypes.byref(max_subset_size_c), ctypes.byref(n_paralogs),",
        "                         ctypes.byref(work_array_size), filtered_paralogs_mask,",
        "                         ctypes.byref(n_mask_chunks), ctypes.byref(ierr))",
        "    check_err_code(ierr.value)",
        "",
        "    return {",
        "        'actual_max_subset_size': max_subset_size_c.value,",
        "        'work_array_size': work_array_size.value",
        "    }",
        ""
      ],
      "description": "Sets up the python function to call tox_calc_work_arr_paralog_subsets_size"
    },

    "Python set up tox_filter_paralogs_by_pattern_dosage_effect": {
      "prefix": "tox:tox_filter_paralogs_by_pattern_dosage_effect_py",
      "body": [
        "def tox_filter_paralogs_by_pattern_dosage_effect(gene_angles, threshold,",
        "                                                 sorted_gene_to_fam_perm,",
        "                                                 perm_first_paralog_idx, n_paralogs):",
        "    \"\"\"",
        "    Filter paralogs by dosage effect using angle threshold, within the grouped slice.",
        "",
        "    Args:",
        "        gene_angles (array-like): Angles for all genes (length = n_genes).",
        "        threshold (float): Filtering threshold.",
        "        sorted_gene_to_fam_perm (array-like): Permutation grouping families (length = n_genes).",
        "        perm_first_paralog_idx (int): Start index of ancestor's family slice.",
        "        n_paralogs (int): Number of paralogs in that slice.",
        "",
        "    Returns:",
        "        np.ndarray: int32 mask (chunks of 32 bits) with 1 for kept, 0 otherwise.",
        "    \"\"\"",
        "    gene_angles = np.ascontiguousarray(gene_angles, dtype=np.float64)",
        "    sorted_gene_to_fam_perm = np.ascontiguousarray(sorted_gene_to_fam_perm, dtype=np.int32)",
        "    n_genes = ctypes.c_int(len(gene_angles))",
        "    threshold_c = ctypes.c_double(float(threshold))",
        "    perm_first_paralog_idx_c = ctypes.c_int(int(perm_first_paralog_idx))",
        "    n_paralogs_c = ctypes.c_int(int(n_paralogs))",
        "",
        "    n_mask_chunks = tox_mask_chunk_count(n_paralogs_c.value)",
        "    n_mask_chunks_c = ctypes.c_int(n_mask_chunks)",
        "    mask = np.empty(n_mask_chunks, dtype=np.int32)",
        "    ierr = ctypes.c_int(0)",
        "",
        "    filter_dosage_effect_c = lib.filter_paralogs_by_pattern_dosage_effect",
        "    filter_dosage_effect_c.argtypes = [",
        "        np.ctypeslib.ndpointer(dtype=np.float64, flags=\"C_CONTIGUOUS\"),",
        "        ctypes.POINTER(ctypes.c_double),",
        "        ctypes.POINTER(ctypes.c_int),",
        "        np.ctypeslib.ndpointer(dtype=np.int32, flags=\"C_CONTIGUOUS\"),",
        "        ctypes.POINTER(ctypes.c_int),",
        "        ctypes.POINTER(ctypes.c_int),",
        "        np.ctypeslib.ndpointer(dtype=np.int32, flags=\"C_CONTIGUOUS\"),",
        "        ctypes.POINTER(ctypes.c_int),",
        "        ctypes.POINTER(ctypes.c_int)",
        "    ]",
        "    filter_dosage_effect_c.restype = None",
        "",
        "    filter_dosage_effect_c(gene_angles, ctypes.byref(threshold_c), ctypes.byref(n_genes),",
        "                           sorted_gene_to_fam_perm, ctypes.byref(perm_first_paralog_idx_c),",
        "                           ctypes.byref(n_paralogs_c), mask, ctypes.byref(n_mask_chunks_c),",
        "                           ctypes.byref(ierr))",
        "    check_err_code(ierr.value)",
        "",
        "    _readonly(mask)",
        "    return mask",
        ""
      ],
      "description": "Sets up the python function to call tox_filter_paralogs_by_pattern_dosage_effect"
    },

    "Python set up tox_filter_paralogs_by_pattern_subfunctionalization": {
      "prefix": "tox:tox_filter_paralogs_by_pattern_subfunctionalization_py",
      "body": [
        "def tox_filter_paralogs_by_pattern_subfunctionalization(gene_angles, threshold,",
        "                                                        sorted_gene_to_fam_perm,",
        "                                                        perm_first_paralog_idx, n_paralogs):",
        "    \"\"\"",
        "    Filter paralogs by subfunctionalization pattern using angle threshold, within the grouped slice.",
        "",
        "    Args:",
        "        gene_angles (np.ndarray): Angles for all genes (length = n_genes).",
        "        threshold (float): Filtering threshold.",
        "        sorted_gene_to_fam_perm (np.ndarray): Permutation grouping families.",
        "        perm_first_paralog_idx (int): Start index of ancestor's family slice.",
        "        n_paralogs (int): Number of paralogs in that slice.",
        "",
        "    Returns:",
        "        np.ndarray: int32 mask (chunks of 32 bits) with 1 for kept, 0 otherwise.",
        "    \"\"\"",
        "    gene_angles = np.ascontiguousarray(gene_angles, dtype=np.float64)",
        "    sorted_gene_to_fam_perm = np.ascontiguousarray(sorted_gene_to_fam_perm, dtype=np.int32)",
        "    n_genes = ctypes.c_int(len(gene_angles))",
        "    threshold_c = ctypes.c_double(float(threshold))",
        "    perm_first_paralog_idx_c = ctypes.c_int(int(perm_first_paralog_idx))",
        "    n_paralogs_c = ctypes.c_int(int(n_paralogs))",
        "",
        "    n_mask_chunks = tox_mask_chunk_count(n_paralogs_c.value)",
        "    n_mask_chunks_c = ctypes.c_int(n_mask_chunks)",
        "    mask = np.empty(n_mask_chunks, dtype=np.int32)",
        "    ierr = ctypes.c_int(0)",
        "",
        "    filter_subfunctionalization_c = lib.filter_paralogs_by_pattern_subfunctionalization_c",
        "    filter_subfunctionalization_c.argtypes = [",
        "        np.ctypeslib.ndpointer(dtype=np.float64, flags=\"C_CONTIGUOUS\"),",
        "        ctypes.POINTER(ctypes.c_double),",
        "        ctypes.POINTER(ctypes.c_int),",
        "        np.ctypeslib.ndpointer(dtype=np.int32, flags=\"C_CONTIGUOUS\"),",
        "        ctypes.POINTER(ctypes.c_int),",
        "        ctypes.POINTER(ctypes.c_int),",
        "        np.ctypeslib.ndpointer(dtype=np.int32, flags=\"C_CONTIGUOUS\"),",
        "        ctypes.POINTER(ctypes.c_int),",
        "        ctypes.POINTER(ctypes.c_int)",
        "    ]",
        "    filter_subfunctionalization_c.restype = None",
        "",
        "    filter_subfunctionalization_c(gene_angles, ctypes.byref(threshold_c), ctypes.byref(n_genes),",
        "                                  sorted_gene_to_fam_perm, ctypes.byref(perm_first_paralog_idx_c),",
        "                                  ctypes.byref(n_paralogs_c), mask, ctypes.byref(n_mask_chunks_c),",
        "                                  ctypes.byref(ierr))",
        "    check_err_code(ierr.value)",
        "",
        "    _readonly(mask)",
        "    return mask",
        ""
      ],
      "description": "Sets up the python function to call tox_filter_paralogs_by_pattern_subfunctionalization"
    },

    "Python set up tox_detect_subfunctionalization": {
      "prefix": "tox:tox_detect_subfunctionalization_py",
      "body": [
        "def tox_detect_subfunctionalization(ancestor, genes, rdi_threshold,",
        "                                    sorted_gene_to_fam_perm, perm_first_paralog_idx, n_paralogs,",
        "                                    filtered_paralogs_mask, max_subset_size,",
        "                                    paralog_norms, sorted_paralog_norms_perm):",
        "    \"\"\"",
        "    Detect subfunctionalization among paralogs based on residual distance and pruning.",
        "",
        "    Args:",
        "        ancestor (np.ndarray): Vector (n_dims).",
        "        genes (np.ndarray): 2D array (n_dims, n_genes).",
        "        rdi_threshold (float): Max allowed residual distance.",
        "        sorted_gene_to_fam_perm (np.ndarray): Permutation grouping families.",
        "        perm_first_paralog_idx (int): Slice start in permutation.",
        "        n_paralogs (int): Number of paralogs in that slice.",
        "        filtered_paralogs_mask (np.ndarray): int32 mask.",
        "        max_subset_size (int): Desired max subset size.",
        "        paralog_norms (np.ndarray): float64 norms for all genes.",
        "        sorted_paralog_norms_perm (np.ndarray): int32 permutation of norms.",
        "",
        "    Returns:",
        "        dict: { 'n_results': int, 'results': np.ndarray }",
        "    \"\"\"",
        "    ancestor = np.asfortranarray(ancestor, dtype=np.float64)",
        "    genes = np.asfortranarray(genes, dtype=np.float64)",
        "    sorted_gene_to_fam_perm = np.ascontiguousarray(sorted_gene_to_fam_perm, dtype=np.int32)",
        "    filtered_paralogs_mask = np.ascontiguousarray(filtered_paralogs_mask, dtype=np.int32)",
        "    paralog_norms = np.ascontiguousarray(paralog_norms, dtype=np.float64)",
        "    sorted_paralog_norms_perm = np.ascontiguousarray(sorted_paralog_norms_perm, dtype=np.int32)",
        "",
        "    n_dims = ctypes.c_int(ancestor.shape[0])",
        "    n_genes = ctypes.c_int(genes.shape[1])",
        "    n_mask_chunks = ctypes.c_int(len(filtered_paralogs_mask))",
        "    rdi_threshold_c = ctypes.c_double(float(rdi_threshold))",
        "    perm_first_paralog_idx_c = ctypes.c_int(int(perm_first_paralog_idx))",
        "    n_paralogs_c = ctypes.c_int(int(n_paralogs))",
        "    ierr = ctypes.c_int(0)",
        "",
        "    work_info = tox_calc_work_arr_paralog_subsets_size(max_subset_size, n_paralogs_c.value, filtered_paralogs_mask)",
        "    max_subset_size_c = ctypes.c_int(work_info[\"actual_max_subset_size\"])",
        "    n_paralog_subsets_c = ctypes.c_int(work_info[\"work_array_size\"])",
        "",
        "    work_arr_paralog_subsets = np.empty((n_mask_chunks.value, n_paralog_subsets_c.value), dtype=np.int32, order='F')",
        "    active_mask = np.empty(n_mask_chunks.value, dtype=np.int32)",
        "    temp_paralog_vector = np.empty(n_dims.value, dtype=np.float64)",
        "    temp_work_array = np.empty(n_genes.value, dtype=np.float64)",
        "    n_results = ctypes.c_int(0)",
        "",
        "    detect_subfunc_c = lib.detect_subfunctionalization_c",
        "    detect_subfunc_c.argtypes = [...]", 
        "    detect_subfunc_c.restype = None",
        "",
        "    detect_subfunc_c(ancestor, genes, ctypes.byref(n_genes), ctypes.byref(n_dims),",
        "                     ctypes.byref(rdi_threshold_c),",
        "                     sorted_gene_to_fam_perm, ctypes.byref(perm_first_paralog_idx_c),",
        "                     ctypes.byref(n_paralogs_c), filtered_paralogs_mask, ctypes.byref(n_mask_chunks),",
        "                     ctypes.byref(n_results), ctypes.byref(max_subset_size_c),",
        "                     work_arr_paralog_subsets, ctypes.byref(n_paralog_subsets_c),",
        "                     active_mask, temp_paralog_vector, paralog_norms,",
        "                     sorted_paralog_norms_perm, temp_work_array, ctypes.byref(ierr))",
        "    check_err_code(ierr.value)",
        "",
        "    results = work_arr_paralog_subsets[:, :n_results.value].copy()",
        "    _readonly(results)",
        "    return { 'n_results': n_results.value, 'results': results }",
        ""
      ],
      "description": "Sets up the python function to call tox_detect_subfunctionalization"
    },

    "Python set up tox_detect_dosage_effect": {
      "prefix": "tox:tox_detect_dosage_effect_py",
      "body": [
        "def tox_detect_dosage_effect(ancestor, genes,",
        "                             sorted_gene_to_fam_perm, perm_first_paralog_idx, n_paralogs,",
        "                             filtered_paralogs_mask, max_subset_size,",
        "                             gain_gamma=0.1, max_angle=np.pi):",
        "    \"\"\"",
        "    Detect dosage effect among paralogs using gain and angle thresholds.",
        "",
        "    Args:",
        "        ancestor (np.ndarray): Vector (n_dims).",
        "        genes (np.ndarray): 2D array (n_dims, n_genes).",
        "        sorted_gene_to_fam_perm (np.ndarray): Permutation grouping families.",
        "        perm_first_paralog_idx (int): Slice start in permutation for ancestor's family.",
        "        n_paralogs (int): Number of paralogs in that slice.",
        "        filtered_paralogs_mask (np.ndarray): int32 mask for n_genes.",
        "        max_subset_size (int): Desired max subset size.",
        "        gain_gamma (float): Required magnitude gain (default 0.1).",
        "        max_angle (float): Maximum allowed angle in radians (default π).",
        "",
        "    Returns:",
        "        dict: { 'n_results': int, 'results': np.ndarray }",
        "    \"\"\"",
        "    ancestor = np.asfortranarray(ancestor, dtype=np.float64)",
        "    genes = np.asfortranarray(genes, dtype=np.float64)",
        "    sorted_gene_to_fam_perm = np.ascontiguousarray(sorted_gene_to_fam_perm, dtype=np.int32)",
        "    filtered_paralogs_mask = np.ascontiguousarray(filtered_paralogs_mask, dtype=np.int32)",
        "",
        "    n_dims = ctypes.c_int(ancestor.shape[0])",
        "    n_genes = ctypes.c_int(genes.shape[1])",
        "    n_mask_chunks = ctypes.c_int(len(filtered_paralogs_mask))",
        "    perm_first_paralog_idx_c = ctypes.c_int(int(perm_first_paralog_idx))",
        "    n_paralogs_c = ctypes.c_int(int(n_paralogs))",
        "    max_angle_c = ctypes.c_double(float(max_angle))",
        "    gain_gamma_c = ctypes.c_double(float(gain_gamma))",
        "    ierr = ctypes.c_int(0)",
        "",
        "    work_info = tox_calc_work_arr_paralog_subsets_size(max_subset_size, n_paralogs_c.value, filtered_paralogs_mask)",
        "    max_subset_size_c = ctypes.c_int(work_info[\"actual_max_subset_size\"])",
        "    n_paralog_subsets_c = ctypes.c_int(work_info[\"work_array_size\"])",
        "",
        "    work_arr_paralog_subsets = np.empty((n_mask_chunks.value, n_paralog_subsets_c.value), dtype=np.int32, order='F')",
        "    active_mask = np.empty(n_mask_chunks.value, dtype=np.int32)",
        "    temp_paralog_vector = np.empty(n_dims.value, dtype=np.float64)",
        "    n_results = ctypes.c_int(0)",
        "",
        "    detect_dosage_effect_c = lib.detect_dosage_effect_c",
        "    detect_dosage_effect_c.argtypes = [...]", 
        "    detect_dosage_effect_c.restype = None",
        "",
        "    detect_dosage_effect_c(ancestor, genes, ctypes.byref(n_genes), ctypes.byref(n_dims),",
        "                           sorted_gene_to_fam_perm, ctypes.byref(perm_first_paralog_idx_c),",
        "                           ctypes.byref(n_paralogs_c), filtered_paralogs_mask, ctypes.byref(n_mask_chunks),",
        "                           ctypes.byref(n_results), ctypes.byref(max_subset_size_c),",
        "                           work_arr_paralog_subsets, ctypes.byref(n_paralog_subsets_c),",
        "                           active_mask, temp_paralog_vector,",
        "                           ctypes.byref(max_angle_c), ctypes.byref(gain_gamma_c),",
        "                           ctypes.byref(ierr))",
        "    check_err_code(ierr.value)",
        "",
        "    results = work_arr_paralog_subsets[:, :n_results.value].copy()",
        "    _readonly(results)",
        "    return { 'n_results': n_results.value, 'results': results }",
        ""
      ],
      "description": "Sets up the python function to call tox_detect_dosage_effect"
    }
}